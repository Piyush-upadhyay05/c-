//C++ has many functions that allows you to perform mathematical tasks on numbers math function is one of them
#include<iostream>
#include <cmath>
using namespace std;
int main()
{
cout << max(5, 10)<<endl;//max function is used to 
cout << min(5, 10)<<endl; // min is used to find the smallest number written in parenthses .
cout << sqrt(64)<<endl; // square root
cout << round(2.6)<<endl; // round of number 
cout << log(2)<<endl; // logrithm
return 0;
}